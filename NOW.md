# NOW

## Current Goal
Зафиксировать результат реального upstream update на отдельной ветке и сохранить рабочее operational rule для oauth-safe push без `.github/workflows/*`.

## Current Upstream Update
- Работа идёт в `codex-rs` на ветке `chore/update-upstream-on-gena-arch`.
- Ветка запушена в `origin` и сейчас используется как dedicated branch для апдейта апстрима.
- Baseline architecture checkpoint для новой ветки:
  - `4c8e65155` — `refactor(gena): split runtime bootstrap onboarding module`
- Merge на ветке завершён:
  - `upstream/main -> chore/update-upstream-on-gena-arch`
- Merge commit:
  - `9bc9e5253` — `Merge upstream/main into chore/update-upstream-on-gena-arch`
- Divergence перед merge:
  - `upstream/main`: `105` commits
  - `chore/update-upstream-on-gena-arch`: `187` commits
- Push полного diff через OAuth remote потребовал workaround:
  - workflow-файлы `.github/workflows/*` были дропнуты отдельным follow-up commit
  - `ae5f2141f` — `sync(upstream): drop workflow changes for oauth push`
- Ветка успешно синхронизирована с `origin/chore/update-upstream-on-gena-arch`.
- Практический вывод на этом checkpoint:
  - merge pressure есть, но он не разъехался хаотично по всему `gena-runtime`
  - это уже первый реальный сигнал, что многодневная runtime/platform normalization была не зря

## State
- Работа шла в `codex-rs` на ветке `chore/update-arch`; phase закрыта и зафиксирована как baseline.
- Новый практический фронт сейчас:
  - `codex-rs/chore/update-upstream-on-gena-arch`
- Актуальный кодовый checkpoint:
  - `4c8e65155` — `refactor(gena): split runtime bootstrap onboarding module`
  - `c3c1cf1e9` — `refactor(gena): split runtime config session module`
  - `88a9a60be` — `refactor(gena): split runtime capability axis modules`
  - `03e793737` — `refactor(gena): split runtime registry parser module`
  - `cef80019f` — `refactor(gena): split runtime transport module`
  - `84e3da007` — `refactor(gena): split runtime loader module`
  - `727667207` — `refactor(gena): split runtime execution module`
  - `cbcf72943` — `refactor(gena): split runtime capabilities module`
  - `f6bde621e` — `refactor(gena): split runtime registry service module`
  - `9b62600e0` — `refactor(gena): split runtime package archive module`
  - `a2f0f4627` — `refactor(gena): split runtime registry model module`
  - `9b62600e0` — `refactor(gena): split runtime package archive module`
  - `f6bde621e` — `refactor(gena): split runtime registry service module`
  - `cbcf72943` — `refactor(gena): split runtime capabilities module`
  - `727667207` — `refactor(gena): split runtime execution module`
  - `84e3da007` — `refactor(gena): split runtime loader module`
  - `cef80019f` — `refactor(gena): split runtime transport module`
  - `31afb59bf` — `refactor(gena): split runtime package module`
  - `59f6a8ac2` — `refactor(gena): split runtime registry client module`
  - `b037dbd67` — `refactor(gena): normalize paginator response view`
  - `b5764bafa` — `feat(gena): enrich paginator response metadata`
  - `c52db4bb3` — `feat(gena): add full page iteration for registry search`
  - `168d9eace` — `feat(gena): add next-page search navigation`
  - `7ad1759fa` — `feat(gena): add cursor-aware registry search`
  - `18c00538f` — `feat(gena): add paged search responses`
  - `5f62b31b1` — `feat(gena): add registry search paging`
  - `fa260a179` — `feat(gena): add registry service auth`
  - `115ff1e57` — `feat(gena): add package detail inspection`
  - `ebdacc752` — `feat(gena): add service-backed package resolution`
  - `5a0668f25` — `feat(gena): add service-backed registry search`
  - `353608950` — `feat(gena): add registry service endpoints`
  - `acfb22acf` — `feat(gena): add index service capabilities`
  - `a9920d32a` — `feat(gena): add plugin index manifest`
  - `986cdb28f` — `feat(gena): extend plugin registry contract`
  - `54fb940fb` — `feat(gena): enrich plugin registry metadata`
  - `18823ebd0` — `feat(gena): add fuzzy plugin search`
  - `a36ee1d93` — `feat(gena): rank plugin index search results`
  - `7fd710139` — `feat(gena): add plugin index search ux`
  - `56d8fa220` — `feat(gena): add registry-backed plugin management`
  - `5ded103dc` — `feat(gena): add registry-backed plugin install`
  - `ae7d6f7ef` — `feat(gena): add remote plugin index inspection`
  - `149d4093b` — `feat(gena): add remote plugin archive management`
  - `0cc563f32` — `feat(gena): add remote plugin archive install`
  - `88dde7711` — `feat(gena): add package distribution metadata`
  - `fe82e6a97` — `feat(gena): enrich archive installer output`
  - `f9693fcac` — `feat(gena): add plugin archive inspection`
  - `84b100596` — `feat(gena): add richer archive metadata`
  - `a5877a023` — `feat(gena): add archive package manifest`
  - `92400f4ec` — `feat(gena): add plugin archive uninstall flow`
  - `fdbac73d5` — `build(gena): sync lockfile for archive uninstall`
- Текущая phase считается **architecturally complete**:
  - Gena-owned runtime/platform layers собраны
  - plugin platform имеет capability axes, execution stack, lifecycle layer, dynamic loader и registry/distribution surface
  - `gena-runtime` нормализован в отдельные boundary-модули с минимальным remaining structural pressure
- Новые code splits после `bootstrap_onboarding` уже дают diminishing returns и не являются обязательными для закрытия текущей задачи.
- Что считаем выполненным в рамках этой фазы:
  - migration/platform buildout доведён до production-usable checkpoint
  - plugin platform expansion доведён до registry/distribution/execution/lifecycle checkpoint
  - runtime module boundaries разложены до уровня, достаточного для честного closeout
- Что сознательно остаётся вне scope этой фазы:
  - новые feature frontiers поверх platform
  - richer product UX beyond current runtime/platform scope
  - дальнейшая platform evolution без явного нового pressure
- Plugin platform теперь имеет runtime-owned dynamic loader с compatibility manifest validation, runtime reporting, publishing metadata, directory-based discovery, canonical install location, complete local install/replace/uninstall loop, bundle-directory convention, bundle metadata validation, local archive install/update/uninstall UX и richer archive package contract:
- В `gena-runtime` теперь выделены два явных platform boundary-модуля:
  - `src/execution.rs`
    - runtime snapshot
    - lifecycle manager
    - prepared execution
    - lifecycle trace
    - execution target / execution plan façade
  - `src/capabilities.rs`
    - thin facade and re-export layer
  - `src/capability_command.rs`
    - command dispatch plan
    - command resolve/require helpers
    - command registry-based planning
  - `src/capability_provider.rs`
    - provider execution plan
    - provider resolve/require helpers
    - provider registry-based planning
  - `src/capability_tool.rs`
    - tool execution plan
    - tool resolve/require helpers
    - tool registry-based planning
  - `src/capability_workflow.rs`
    - workflow execution plan
    - workflow resolve/require helpers
    - workflow registry-based planning
  - `src/config_session.rs`
    - session target resolution
    - latest session lookup
    - thread id resolution
    - session cwd lookup
    - turn-context cwd parsing
  - `src/bootstrap_onboarding.rs`
    - trust/login/onboarding gating
    - provider token prepare/persist
    - TUI-only bootstrap onboarding policy
  - `src/registry_service.rs`
    - service-backed search
    - auth-aware service fetch
    - service-backed package resolution
    - service-side paging/navigation request construction
  - `src/registry_model.rs`
    - registry-facing structs
    - search result/page/page-info types
    - resolved package types
    - search scoring and fuzzy ranking helpers
  - `src/loader.rs`
    - static built-in registry init
    - dynamic loader discovery
    - env/configured library and directory resolution
    - dynamic plugin registration
    - dynamic manifest / bundle validation
  - `src/transport.rs`
    - shared HTTP response helper
    - text/bytes download helpers
    - auth-env bearer handling
    - shared `http://` / `https://` validation
  - `src/registry.rs`
    - registry/index inspect
    - static search facade
    - install/replace/uninstall facade
    - high-level registry facade
  - `src/registry_parser.rs`
    - static TOML index parsing
    - `[index]` / `[service]` / `[response]` validation
    - `[[packages]]` parsing
    - package entry parsing and URL policy checks
  - `src/package.rs`
    - local library install/replace/uninstall
    - local bundle install/replace/uninstall
    - bundle discovery and validation helpers
    - high-level package facade
  - `src/package_archive.rs`
    - archive inspect/install/replace/uninstall
    - remote archive URL install/inspect/replace/uninstall
    - archive manifest parsing/validation
    - extracted bundle resolution
  - `src/plugins.rs` после шести split’ов сведён к thin compatibility shell
  - в `gena-plugin-api` добавлены:
    - `DynamicPluginRegisterFn`
    - `DYNAMIC_PLUGIN_REGISTER_SYMBOL`
    - `DynamicPluginManifest`
    - `DynamicPluginManifestFn`
    - `DYNAMIC_PLUGIN_MANIFEST_SYMBOL`
    - `CURRENT_DYNAMIC_PLUGIN_ABI_VERSION`
    - publishing-facing manifest fields:
      - `plugin_version`
      - `repository_url`
      - `license`
  - в `gena-runtime` добавлены:
    - `initialize_dynamic_runtime_plugin_registry(...)`
    - `initialize_discovered_runtime_plugin_registry_with_dynamic_loader(...)`
    - `RuntimePluginSnapshot::discover_with_dynamic_loader(...)`
    - `RuntimePluginLifecycle::discover_with_dynamic_loader(...)`
    - dynamic manifest validation before plugin registration
    - `RuntimeDynamicPlugin`
    - `RuntimePluginSnapshot::dynamic_plugins()`
  - dynamic loader opt-in и env-driven:
    - `GENA_PLUGIN_LIBRARIES`
    - `GENA_PLUGIN_DIRS`
  - dynamic loader теперь умеет:
    - default brand-aware plugin install location:
      - `<product_home>/plugins`
    - non-recursive discovery shared libraries from configured plugin directories
    - platform-aware extension filtering (`.so` / `.dylib` / `.dll`)
    - plugin bundle directories with `gena-plugin.toml`
      - bundle manifest fields:
        - `plugin_id = "..."`
        - `display_name = "..."`
        - `plugin_version = "..."`
        - `library = "..."`
    - archive package manifest with `gena-package.toml`
      - package manifest fields:
        - `package_id = "..."`
        - `display_name = "..."`
        - `package_version = "..."`
        - `distribution_url = "..."`
        - `bundle_dir = "..."`
  - в `gena-runtime` добавлен minimal install helper:
    - `install_runtime_plugin_library(...)`
    - `uninstall_runtime_plugin_library(...)`
    - `replace_runtime_plugin_library(...)`
    - `install_runtime_plugin_bundle(...)`
    - `install_runtime_plugin_bundle_archive(...)`
    - `inspect_runtime_plugin_bundle_archive(...)`
    - `replace_runtime_plugin_bundle(...)`
    - `replace_runtime_plugin_bundle_archive(...)`
    - `uninstall_runtime_plugin_bundle_archive(...)`
  - в `codex-cli` добавлен first install command path:
    - `debug install-plugin <LIBRARY_PATH>`
    - `debug uninstall-plugin <LIBRARY_PATH_OR_NAME>`
    - `debug replace-plugin <LIBRARY_PATH>`
    - `debug install-plugin-bundle <BUNDLE_DIR>`
    - `debug uninstall-plugin-bundle <BUNDLE_DIR_OR_NAME>`
    - `debug install-plugin-archive <ARCHIVE_PATH>`
    - `debug inspect-plugin-archive <ARCHIVE_PATH>`
    - `debug replace-plugin-archive <ARCHIVE_PATH>`
    - `debug uninstall-plugin-archive <ARCHIVE_PATH>`
- `gena-plugins-core` теперь экспортирует canonical registrar symbol:
    - `gena_register_plugin`
    - `gena_plugin_manifest`
    - crate собирается как `rlib + cdylib`
  - production discovery callsite'ы переведены на dynamic-loader-aware path:
    - `codex mcp`
    - `codex review`
    - `codex debug plugins`
    - `codex-exec`
    - `codex-tui`
    - `tui/chatwidget`
  - `codex debug plugins` теперь печатает discovered dynamic plugin manifests:
    - `plugin_id`
    - `plugin_display_name`
    - `abi_version`
    - `plugin_api_version`
    - `plugin_version`
    - `license`
    - `repository_url`
    - `library_path`
    - `bundle_root`
    - bundle metadata:
      - `bundle_plugin_id`
      - `bundle_display_name`
      - `bundle_version`
- bundle metadata теперь валидируется против dynamic library manifest:
  - bundle `plugin_id`
  - bundle `display_name`
  - bundle `plugin_version`
- Практический смысл:
  - package management boundary теперь локализован в отдельном runtime module, а не размазан внутри `plugins.rs`
  - execution/lifecycle façade теперь тоже локализован в отдельном runtime module, а не смешан с capability planning
  - capability planning теперь тоже локализован в отдельном runtime module, а не смешан с execution/lifecycle surface
  - loader/discovery/registration теперь тоже локализован в отдельном runtime module, а не размазан внутри `plugins.rs`
  - transport/network policy теперь тоже локализован в отдельном runtime module, а не продублирован между `registry.rs` и `package.rs`
  - evolution installer/archive/remote package story теперь может идти отдельно от execution/lifecycle story
  - после `registry.rs` + `registry_service.rs` + `registry_model.rs` + `package.rs` + `package_archive.rs` + `transport.rs` + `loader.rs` + `execution.rs` + `capabilities.rs` split’ов общий runtime platform слой стал заметно чище по ответственности
  - plugin bundle convention теперь задаёт не только layout, но и identity/version contract
  - dynamic loader уже умеет не просто найти bundle, но и проверить согласованность bundle metadata с library manifest
  - archive UX теперь уже замкнут локально:
    - inspect
    - install
    - replace/update
    - uninstall
  - `gena-package.toml` теперь задаёт archive-level identity/version contract, а old one-bundle fallback остаётся backward-compatible
  - archive package contract теперь включает first remote-facing metadata field:
    - `distribution_url`
    - runtime валидирует только supported remote schemes:
      - `http://`
      - `https://`
    - `debug inspect-plugin-archive` теперь печатает distribution source
  - появился первый actual remote fetch/install path:
    - `install_runtime_plugin_bundle_archive_url(...)`
    - `inspect_runtime_plugin_bundle_archive_url(...)`
    - CLI:
      - `debug install-plugin-url <ARCHIVE_URL>`
  - remote archive management теперь симметричен:
    - `replace_runtime_plugin_bundle_archive_url(...)`
    - `uninstall_runtime_plugin_bundle_archive_url(...)`
    - CLI:
      - `debug replace-plugin-url <ARCHIVE_URL>`
      - `debug uninstall-plugin-url <ARCHIVE_URL>`
  - появился first read-only remote registry/index path:
    - `inspect_runtime_plugin_registry_index_url(...)`
    - CLI:
      - `debug inspect-plugin-index <INDEX_URL>`
    - current index contract:
      - `[[packages]]`
      - `package_id`
      - `display_name`
      - `package_version`
      - `distribution_url`
  - registry story теперь уже не read-only:
    - `install_runtime_plugin_from_registry_index(...)`
    - CLI:
      - `debug install-plugin-from-index <INDEX_URL> <PACKAGE_ID>`
  - registry-backed management loop теперь симметричен:
    - `replace_runtime_plugin_from_registry_index(...)`
    - `uninstall_runtime_plugin_from_registry_index(...)`
    - CLI:
      - `debug replace-plugin-from-index <INDEX_URL> <PACKAGE_ID>`
      - `debug uninstall-plugin-from-index <INDEX_URL> <PACKAGE_ID>`
  - search/discovery UX теперь тоже есть:
    - `search_runtime_plugin_registry_index(...)`
    - CLI:
      - `debug search-plugin-index <INDEX_URL> <QUERY>`
  - search results теперь ranked:
    - exact `package_id`
    - exact `display_name`
    - prefix match
    - substring match
    - CLI search output теперь печатает `score`
  - fuzzy search fallback теперь тоже есть:
    - subsequence match for `package_id`
    - subsequence match for `display_name`
    - lower score than exact/prefix/substring matches
  - remote registry/index contract теперь богаче:
    - top-level `[index]` manifest:
      - `format_version`
      - `registry_id`
      - `display_name`
      - `capabilities`
    - optional `[service]`:
      - `search_url`
      - `package_url_template`
      - `auth_env`
      - `page_param`
      - `page_size_param`
      - `cursor_param`
    - `publisher`
    - `license`
    - `repository_url`
    - `summary`
    - `capabilities`
    - `tags`
  - runtime parser/validator теперь проверяет:
    - required `[index]`
    - supported `format_version = "1"`
    - non-empty `[index].capabilities`
    - supported `[service].search_url`
    - supported `[service].package_url_template`
    - `{package_id}` placeholder in `package_url_template`
    - optional non-empty `[service].auth_env`
    - non-empty `publisher`
    - non-empty `license`
    - supported `repository_url` schemes:
      - `http://`
      - `https://`
    - non-empty `summary`
    - non-empty `capabilities`
    - all `capabilities` / `tags` entries are non-empty strings
  - `codex-cli` теперь печатает richer registry entry/search output:
    - top-level `plugin-index-manifest`
  - service-backed registry search теперь cursor-aware:
    - optional `[response]`
      - `next_cursor`
    - runtime helper:
      - `search_runtime_plugin_registry_index_with_navigation(...)`
    - `search_runtime_plugin_registry_index_with_paging(...)` сохранён как совместимый wrapper
    - `codex-cli` path:
      - `debug search-plugin-index <INDEX_URL> <QUERY> --cursor <CURSOR>`
    - CLI search output теперь печатает `plugin-search-page` header с:
      - `page`
      - `page_size`
      - `total`
      - `next_page`
      - `next_cursor`
  - runtime page navigation now has explicit next-request shaping:
    - `resolve_runtime_plugin_search_next_request(...)`
    - `RuntimePluginSearchNextRequest`
    - CLI search output теперь печатает:
      - `plugin-search-next`
        - `page`
        - `page_size`
        - `cursor`
    - следующий page request теперь можно строить из runtime-owned contract, а не из ad-hoc CLI логики
  - runtime paginator layer now includes full page iteration:
    - `RuntimePluginSearchIteration`
    - `search_runtime_plugin_registry_index_all_pages(...)`
    - loop uses runtime-owned:
      - `page`
      - `page_size`
      - `cursor`
      - `next_page`
      - `next_cursor`
    - loop is guarded by:
      - repeated-request detection
      - `max_pages`
    - `codex-cli` search path now supports:
      - `--all-pages`
      - `--max-pages`
    - CLI prints:
      - `plugin-search-iteration`
      - then page-by-page:
        - `plugin-search-page`
        - `plugin-search-next`
        - `plugin-search-entry`
  - paginator response contract теперь богаче:
    - optional `[response]`
      - `returned`
      - `has_more`
    - `RuntimePluginRegistryResponse` now includes:
      - `returned`
      - `has_more`
    - CLI `plugin-search-page` output теперь печатает:
      - `page`
      - `page_size`
      - `total`
      - `returned`
      - `has_more`
      - `next_page`
      - `next_cursor`
  - paginator response now has a runtime-owned normalized view:
    - `RuntimePluginSearchPageInfo`
    - `inspect_runtime_plugin_search_page(...)`
    - normalized page info includes:
      - `page`
      - `page_size`
      - `total`
      - `returned`
      - `has_more`
      - `next_request`
    - CLI больше не разбирает raw response fields вручную
  - runtime registry/service client boundary теперь вынесен в отдельный module:
    - `gena-runtime/src/registry.rs`
    - туда перенесены:
      - registry/service fetch
      - auth-aware text download
      - static index parsing
      - service-backed search
      - package resolution
      - paginator/search normalization types
    - `gena-runtime/src/plugins.rs` оставлен за:
      - dynamic loader
      - plugin execution/lifecycle
      - installer/archive/bundle paths
    - top-level index capabilities
    - optional service endpoints
    - `summary`
    - `publisher`
    - `license`
    - `capabilities`
    - `tags`
    - `repository_url`
  - search/discovery UX теперь опирается и на service/discovery fields:
    - exact / prefix / substring match по `capabilities`
    - exact / prefix / substring match по `tags`
    - substring + fuzzy fallback по `summary`
  - появился first service-backed registry path:
    - search uses `[service].search_url?q=...` when declared
    - static index search remains fallback
    - search result now reports source:
      - `index`
      - `service`
  - package resolution теперь тоже service-backed when available:
    - resolver uses `[service].package_url_template`
    - reads `[package]` TOML document
    - validates resolved `package_id`
    - keeps static index fallback if endpoint not declared
    - install / replace / uninstall from index now report source:
      - `index`
      - `service`
  - появился explicit package detail path:
    - `inspect_runtime_plugin_from_registry_index(...)`
    - CLI:
      - `debug inspect-plugin-from-index <INDEX_URL> <PACKAGE_ID>`
  - service-backed search/package resolution теперь auth-aware when configured:
    - bearer token comes from `[service].auth_env`
    - static index fallback stays unauthenticated and unchanged
  - service-backed search now supports request-side paging when declared:
    - runtime helper:
      - `search_runtime_plugin_registry_index_with_paging(...)`
    - CLI:
      - `debug search-plugin-index --page ... --page-size ...`
    - manifest inspection now prints:
      - `page_param`
      - `page_size_param`
  - paged search now supports optional response metadata:
    - `[response]`
      - `page`
      - `page_size`
      - `total`
      - `next_page`
    - runtime returns structured:
      - `RuntimePluginSearchPage`
      - `RuntimePluginRegistryResponse`
    - CLI prints `plugin-search-page` header when metadata is present
  - installer UX теперь включает preview path для package/bundle metadata до install
  - archive action output теперь package-aware для:
    - install
    - replace/update
    - uninstall
  - `codex-cli` теперь печатает на archive actions:
    - `package_id`
    - `package_version`
    - `bundle_dir`
    - resulting filesystem path
  - local archive installer workflow теперь не только mutates filesystem, но и возвращает package-aware operator-visible result
- Текущий plugin execution stack теперь включает:
  - static discovery
  - dynamic loader
  - compatibility manifest validation
  - runtime snapshot
  - lifecycle manager
  - unified execution planning
  - prepared execution
  - lifecycle trace
  - real execution paths для:
    - `command` (`mcp`)
    - `workflow` (`review`)
    - `tool` (`apply_patch`)
    - `provider` (`llmops`)

## Blockers
- Критических blockers нет.

## Next Step
- Если делать следующий upstream sync через текущий OAuth remote, повторять oauth-safe схему: после merge дропать `.github/workflows/*` отдельным commit перед push.
